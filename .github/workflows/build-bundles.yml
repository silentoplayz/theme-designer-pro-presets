name: Validate & Build Bundles

on:
  push:
    branches: [main]
    paths:
      - 'canvas-fx/**'
      - 'css-presets/**'
      - 'themes/**'
      - 'gradients/**'
      - 'schemas/**'
      - 'scripts/**'
  pull_request:
    paths:
      - 'canvas-fx/**'
      - 'css-presets/**'
      - 'themes/**'
      - 'gradients/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate presets
        run: node scripts/validate.js

  build:
    needs: validate
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build bundles
        run: node scripts/build-bundles.js

      - name: Check for changes
        id: diff
        run: |
          git diff --quiet bundles/ && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit updated bundles
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add bundles/
          git commit -m "chore: auto-regenerate bundles [skip ci]"
          git push
